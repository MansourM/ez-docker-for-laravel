version: '3.9'

services:

  laravel-fpm-server: #inner port :9000
    build:
      context: .
      dockerfile: fpm.Dockerfile
      args:
        - APP_ENV=test
    environment:
      - APP_ENV=test
      - APP_DEBUG=true
      - ./laravel-storage-app:/var/www/storage/app
    restart: unless-stopped
    ports:
      - 9011:9000
    working_dir: /var/www

networks:
  default:
    external: true
    name: ez-shared-network
